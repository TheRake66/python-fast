[project]
name = "Fast"
version = "1.2.1"

[project.scripts]
fast = "program:main"

[tool.setuptools]
py-modules = ["program"]
packages = ["libraries", "commands", "modules", "settings", "templates"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
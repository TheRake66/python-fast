[project]
name = "fast"
version = "0.1.0"

[project.scripts]
fast = "program:main"

[tool.setuptools]
py-modules = ["program"]
packages = ["libraries", "commands"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
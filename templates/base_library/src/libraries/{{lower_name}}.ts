import type { {{capitalize_name}}Type } from '../types/{{lower_name}}.type';

export function init{{capitalize_name}}(): void {
  
}

export function set{{capitalize_name}}({{lower_name}}: {{capitalize_name}}Type): void {
  
}

export function get{{capitalize_name}}(): {{capitalize_name}}Type {
  
}
import type { {{capitalize_item_name}}Type } from '../types/{{lower_item_name}}.type';

export function init{{capitalize_item_name}}(): void {
  
}

export function set{{capitalize_item_name}}({{lower_item_name}}: {{capitalize_item_name}}Type): void {
  
}

export function get{{capitalize_item_name}}(): {{capitalize_item_name}}Type {
  
}